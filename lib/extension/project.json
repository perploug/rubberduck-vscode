{
  "root": "lib/extension",
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "executor": "nx:run-commands",
      "options": {
        "cwd": "lib/extension",
        "command": "npm run compile"
      }
    }
  }
}
